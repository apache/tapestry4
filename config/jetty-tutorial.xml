<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- $Id$ -->
<!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure 1.1//EN"
 "http://jetty.mortbay.com/configure_1_1.dtd">
<!--
  This configuration file is used to allow the Tutorial to be run directly from
  the main distribution directory (i.e., without having to build the frameworks).
  -->
<Configure class="org.mortbay.http.HttpServer">
  <Call name="addListener">
    <Arg>
      <New class="org.mortbay.http.SocketListener">
        <Set name="Port">8080</Set>
        <Set name="MinThreads">5</Set>
        <Set name="MaxThreads">255</Set>
        <Set name="MaxIdleTimeMs">50000</Set>
      </New>
    </Arg>
  </Call>
  <!-- This writes a log of each incoming request to the default output, which makes
       it easier to see what Tapestry and Jetty are up to. -->
  <Set name="LogSink">
    <New class="org.mortbay.util.WriterLogSink"/>
  </Set>
  
  <Call name="addWebApplication">
    <Arg>/tutorial/*</Arg>
    <Arg>lib/Tutorial.war</Arg>
    <Arg/>
    <Call name="setInitParameter">
    	<Arg>com.primix.tapestry.asset.dir</Arg>
    	<Arg>private</Arg>
    </Call>
    <Call name="setInitParameter">
    	<Arg>com.primix.tapestry.asset.URL</Arg>
    	<Arg>/private</Arg>
    </Call>
  </Call>
  
   <Call name="addContext">
    <Arg>/private/*</Arg>
    <Set name="ResourceBase">private</Set>
    <Set name="ServingResources">TRUE</Set>

  </Call>
  
  
</Configure>
